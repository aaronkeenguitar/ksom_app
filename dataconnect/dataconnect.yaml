specVersion: "v1beta"
serviceId: "ksom_app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "ksom_app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
